---
- name: "Connection au devices"
  hosts: switchs
  gather_facts: no
  become: yes

  tasks:
-   name: ajout multiple users avec  role
    cisco.nxos.nxos_user:
      aggregate:
        - name: netop
        - name: netend
      role: network-operator
    state: present