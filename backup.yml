# Playbook to clear NTP config and apply new servers

 hosts: routers
 become: yes
  tasks:

  - name: configure new NTP server 
    ios_config: 
      lines: 
        - ntp server 4.5.6.7


        


    
